import ReactDOM from 'react-dom/client' 
import App from './App'

// Create a React root using ReactDOM.createRoot(), and render the <App /> component inside the element with id 'root'.
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
    